(function(){'use strict';
const app=document.getElementById('eviaApp'),anchor=document.getElementById('eviaAnchor'),content=document.getElementById('content'),copy=document.getElementById('homeCopy');
const course=window.EVIA_COURSE; const KEY='evia3_task_evidence_v2';
const state={screen:'home',unit:null,stageIndex:0,answers:{},photos:{}};
try{const saved=JSON.parse(localStorage.getItem(KEY)||'{}');Object.assign(state,{answers:saved.answers||{},photos:saved.photos||{}})}catch{}
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
function save(){localStorage.setItem(KEY,JSON.stringify({answers:state.answers,photos:state.photos}))}
function unit(id){return course.units.find(x=>x.id===id)}
function photoKey(uid,si,pi){return uid+'|'+si+'|'+pi}
function answerKey(uid,si){return uid+'|'+si}
function openHome(){state.screen='home';state.unit=null;state.stageIndex=0;app.classList.remove('is-open');content.innerHTML='';copy.style.display='';render()}
anchor.addEventListener('click',()=>{if(state.screen==='home'){app.classList.add('is-open');copy.style.display='none';state.screen='units';render()}else openHome()});
window.back=function(){if(state.screen==='stage'){state.screen='unit';render()}else if(state.screen==='unit'){state.screen='units';state.unit=null;render()}else openHome()};
function render(){if(state.screen==='units')renderUnits();else if(state.screen==='unit')renderUnit();else if(state.screen==='stage')renderStage()}
function renderUnits(){content.innerHTML=`<div class="evia-task-sheet"><div class="evia-course-label">${esc(course.title)}</div><div class="evia-unit-list">${course.units.map((u,i)=>{const done=isComplete(u);return `<button type="button" class="evia-unit-pill ${done?'done':''}" data-unit="${esc(u.id)}"><span>${String(i+1).padStart(2,'0')}</span><b>${esc(u.title)}</b><strong>${done?'✓':'›'}</strong></button>`}).join('')}</div></div>`;content.querySelectorAll('[data-unit]').forEach(b=>b.addEventListener('click',()=>chooseUnit(b.dataset.unit)))}
function isComplete(u){return u.stages.every((s,si)=>{const a=state.answers[answerKey(u.id,si)]?.trim();return a||s.evidence.some((_,pi)=>state.photos[photoKey(u.id,si,pi)])})}
function chooseUnit(id){state.unit=unit(id);state.stageIndex=0;state.screen='unit';render()}
function renderUnit(){const u=state.unit;content.innerHTML=`<div class="evia-task-sheet"><button type="button" class="evia-back" id="unitBack">‹ Units</button><section class="evia-unit-head"><span>Unit ${String(course.units.indexOf(u)+1).padStart(2,'0')}</span><h1>${esc(u.title)}</h1><p>${esc(u.summary)}</p></section><div class="evia-stage-list">${u.stages.map((s,i)=>{const done=stageDone(u,i);return `<button type="button" class="evia-stage-pill ${done?'done':''}" data-stage="${i}"><span>${i+1}</span><b>${esc(s.title)}</b><strong>${done?'✓':'›'}</strong></button>`}).join('')}</div></div>`;document.getElementById('unitBack').addEventListener('click',window.back);content.querySelectorAll('[data-stage]').forEach(b=>b.addEventListener('click',()=>chooseStage(Number(b.dataset.stage))))}
function chooseStage(i){state.stageIndex=i;state.screen='stage';render()}
function stageDone(u,si){const s=u.stages[si];return !!(state.answers[answerKey(u.id,si)]?.trim()||s.evidence.some((_,pi)=>state.photos[photoKey(u.id,si,pi)]))}
function renderStage(){const u=state.unit,s=u.stages[state.stageIndex],si=state.stageIndex,answer=state.answers[answerKey(u.id,si)]||'';content.innerHTML=`<div class="evia-task-sheet stage-sheet"><button type="button" class="evia-back" id="stageBack">‹ ${esc(u.title)}</button><section class="stage-head"><span>${si+1} of ${u.stages.length}</span><h1>${esc(s.title)}</h1></section><div class="photo-grid">${s.evidence.map((e,pi)=>{const key=photoKey(u.id,si,pi),src=state.photos[key];return `<article class="photo-slot"><label class="photo-frame ${src?'has':''}">${src?`<img src="${src}" alt="Evidence photo">`:`<span class="camera-icon">📷</span>`}<strong>${esc(e.show)}</strong><em>${src?'Retake photo':'Tap to take photo'}</em><input type="file" accept="image/*" capture="environment" data-photo="${pi}"></label></article>`}).join('')}</div><section class="stage-text"><label for="stageAnswer">Tell me about the work</label><textarea id="stageAnswer" placeholder="What did you do and how did you do it?">${esc(answer)}</textarea></section><div class="stage-nav"><button type="button" class="secondary" id="stageBackButton">Back</button><button type="button" class="primary" id="stageNextButton">${si===u.stages.length-1?'Finish':'Next →'}</button></div><div class="stage-dots">${u.stages.map((_,i)=>`<i class="${i===si?'current':i<si?'done':''}"></i>`).join('')}</div></div>`;
document.getElementById('stageBack').addEventListener('click',window.back);document.getElementById('stageBackButton').addEventListener('click',window.back);document.getElementById('stageNextButton').addEventListener('click',nextStage);content.querySelectorAll('[data-photo]').forEach(input=>input.addEventListener('change',()=>capturePhoto(input,Number(input.dataset.photo))))}
function capturePhoto(input,pi){const file=input.files&&input.files[0];if(!file)return;const frame=input.closest('.photo-frame');const title=frame?.querySelector('strong')?.textContent||'Evidence photo';const reader=new FileReader();reader.onload=()=>{const src=reader.result;const key=photoKey(state.unit.id,state.stageIndex,pi);state.photos[key]=src;save();if(frame){frame.classList.add('has');frame.querySelector('img')?.remove();const img=document.createElement('img');img.src=src;img.alt='Evidence photo';frame.prepend(img);const em=frame.querySelector('em');if(em)em.textContent='Retake photo';}else render()};reader.readAsDataURL(file)}
function nextStage(event){if(event)event.preventDefault();if(!state.unit)return;const el=document.getElementById('stageAnswer');if(el)state.answers[answerKey(state.unit.id,state.stageIndex)]=el.value.trim();save();if(state.stageIndex<state.unit.stages.length-1){state.stageIndex+=1;render()}else{state.screen='unit';render()}}
render();
})();
